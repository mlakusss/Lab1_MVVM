<UserControl x:Class="Lab1_MVVM.Views.DefaultBindingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Lab1_MVVM.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:DefaultBindingViewModel/>
    </UserControl.DataContext>
    <StackPanel Margin="20">
        <TextBlock Text="Привязка по умолчанию (Default)" FontWeight="Bold" FontSize="16"/>
        <GroupBox Header="Привязка через ViewModel" Margin="0,10">
            <StackPanel>
                <TextBlock Text="Имя:" />
                <TextBox Text="{Binding UserName}" Margin="0,5,0,10"/>
                <TextBlock Text="Статус (обновляется при изменении имени через кнопку):" />
                <TextBlock Text="{Binding Status}" Background="LightYellow" Margin="0,5"/>
                <Button Content="Обновить статус" Command="{Binding UpdateStatusCommand}" Margin="0,5"/>
            </StackPanel>
        </GroupBox>
        <GroupBox Header="Прямая привязка к элементам (без VM)" Margin="0,10">
            <StackPanel>
                <TextBlock Text="Введите текст:" />
                <TextBox x:Name="InputTextBox" Margin="0,5"/>
                <TextBlock Text="Вы ввели:" />
                <TextBlock Text="{Binding Text, ElementName=InputTextBox, Mode=OneWay}" Background="LightCyan"/>
                <TextBlock Text="*Mode=OneWay (по умолчанию для TextBlock)" FontSize="10" Foreground="Gray"/>
            </StackPanel>
        </GroupBox>
    </StackPanel>
</UserControl>

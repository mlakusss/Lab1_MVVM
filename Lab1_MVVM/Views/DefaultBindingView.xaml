<UserControl x:Class="Lab1_MVVM.Views.DefaultBindingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Lab1_MVVM.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:DefaultBindingViewModel/>
    </UserControl.DataContext>
    <StackPanel Margin="20">
        <TextBlock Text="Привязка по умолчанию (Default)" FontWeight="Bold" FontSize="16"/>

        <GroupBox Header="{Binding Source={StaticResource LocalizationService}, Path=[Group_ViaVM]}" Margin="0,10">
            <StackPanel>
                <TextBlock Text="{Binding Source={StaticResource LocalizationService}, Path=[Name_Label]}" />
                <TextBox Text="{Binding UserName}" Margin="0,5,0,10"/>
                <TextBlock Text="{Binding Source={StaticResource LocalizationService}, Path=[Status_Label]}" />
                <TextBlock Text="{Binding Status}" Background="LightYellow" Margin="0,5"/>
                <Button Content="{Binding Source={StaticResource LocalizationService}, Path=[Update_Button]}" 
                        Click="UpdateStatus_Click" Margin="0,5"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="{Binding Source={StaticResource LocalizationService}, Path=[Direct_Group]}" Margin="0,10">
            <StackPanel>
                <TextBlock Text="{Binding Source={StaticResource LocalizationService}, Path=[Enter_Text]}" />
                <TextBox x:Name="InputTextBox" Margin="0,5"/>
                <TextBlock Text="{Binding Source={StaticResource LocalizationService}, Path=[YouEntered]}" />
                <TextBlock Text="{Binding Text, ElementName=InputTextBox, Mode=OneWay}" Background="LightCyan"/>
                <TextBlock Text="{Binding Source={StaticResource LocalizationService}, Path=[OneWay_DefaultNote]}" FontSize="10" Foreground="Gray"/>
            </StackPanel>
        </GroupBox>

        <Button Content="{Binding Source={StaticResource LocalizationService}, Path=[Message_Button]}" 
                Click="ShowMessage_Click" Margin="0,10"/>
    </StackPanel>
</UserControl>
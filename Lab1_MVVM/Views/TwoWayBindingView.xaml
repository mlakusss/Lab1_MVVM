<UserControl x:Class="Lab1_MVVM.Views.TwoWayBindingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Lab1_MVVM.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:TwoWayBindingViewModel/>
    </UserControl.DataContext>
    <StackPanel Margin="20">
        <TextBlock Text="Двухсторонняя привязка (TwoWay)" FontWeight="Bold" FontSize="16"/>

        <GroupBox Header="{DynamicResource TwoWay_Description}" Margin="0,10">
            <StackPanel>
                <TextBlock Text="{DynamicResource TwoWay_Name}" />
                <TextBox Text="{Binding UserName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>
                <TextBlock Text="{DynamicResource TwoWay_Age}" />
                <TextBox Text="{Binding UserAge, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>
                <TextBlock Text="{DynamicResource TwoWay_CurrentValues}" Background="LightGreen" Margin="0,10,0,5"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{DynamicResource Name_Label}" />
                    <TextBlock Text="{Binding UserName}" Margin="5,0,15,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{DynamicResource TwoWay_Age_Label}" />
                    <TextBlock Text="{Binding UserAge}" Margin="5,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="{DynamicResource TwoWay_DirectGroup}" Margin="0,10">
            <StackPanel>
                <TextBlock Text="{DynamicResource TwoWay_EnterText}" />
                <TextBox x:Name="DirectTextBox" Margin="0,5"/>
                <TextBlock Text="{DynamicResource TwoWay_Content}" />
                <TextBlock Text="{Binding Text, ElementName=DirectTextBox, Mode=OneWay}" Background="LightCyan"/>
            </StackPanel>
        </GroupBox>

        <TextBlock Text="{DynamicResource TwoWay_Note}" FontSize="10" Foreground="Gray" Margin="0,10"/>
    </StackPanel>
</UserControl>
<UserControl x:Class="Lab1_MVVM.Views.TwoWayBindingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Lab1_MVVM.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:TwoWayBindingViewModel/>
    </UserControl.DataContext>
    <StackPanel Margin="20">
        <TextBlock Text="Двухсторонняя привязка (TwoWay)" FontWeight="Bold" FontSize="16"/>
        <GroupBox Header="Изменение данных в ViewModel автоматически обновляет UI" Margin="0,10">
            <StackPanel>
                <TextBlock Text="Имя (TwoWay):" />
                <TextBox Text="{Binding UserName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>
                <TextBlock Text="Возраст (TwoWay):" />
                <TextBox Text="{Binding UserAge, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>
                <TextBlock Text="Текущие значения из ViewModel:" Background="LightGreen" Margin="0,10,0,5"/>
                <TextBlock Text="{Binding UserName, StringFormat='Имя: {0}'}" />
                <TextBlock Text="{Binding UserAge, StringFormat='Возраст: {0}'}" />
            </StackPanel>
        </GroupBox>
        <GroupBox Header="Прямая привязка к элементам (без ViewModel)" Margin="0,10">
            <StackPanel>
                <TextBlock Text="Введите текст в TextBox (TwoWay по умолчанию):" />
                <TextBox x:Name="DirectTextBox" Margin="0,5"/>
                <TextBlock Text="Содержимое TextBox (привязка OneWay):" />
                <TextBlock Text="{Binding Text, ElementName=DirectTextBox, Mode=OneWay}" Background="LightCyan"/>
            </StackPanel>
        </GroupBox>
        <TextBlock Text="*При вводе в полях 'Имя' или 'Возраст' значения ниже обновляются мгновенно (TwoWay)" FontSize="10" Foreground="Gray" Margin="0,10"/>
    </StackPanel>
</UserControl>

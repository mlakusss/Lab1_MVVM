<UserControl x:Class="Lab1_MVVM.Views.OneTimeBindingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Lab1_MVVM.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:OneTimeBindingViewModel/>
    </UserControl.DataContext>
    <StackPanel Margin="20">
        <TextBlock Text="Одноразовая привязка (OneTime)" FontWeight="Bold" FontSize="16"/>

        <GroupBox Header="{DynamicResource OneTime_Header}" Margin="0,10">
            <StackPanel>
                <TextBlock Text="{DynamicResource OneTime_OneTimeLabel}" />
                <TextBlock Text="{Binding InitialName, Mode=OneTime}" Background="LightSalmon" Margin="0,5" Padding="5"/>
                <TextBlock Text="{DynamicResource OneTime_NormalLabel}" />
                <TextBlock Text="{Binding DynamicName}" Background="LightGreen" Margin="0,5" Padding="5"/>
                <Button Content="{DynamicResource OneTime_Button}" Click="UpdateDynamicName_Click" Margin="0,10,0,0"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="{DynamicResource Explanation_Header}" Margin="0,10">
            <TextBlock TextWrapping="Wrap" Text="{DynamicResource OneTime_Explanation}"/>
        </GroupBox>
    </StackPanel>
</UserControl>
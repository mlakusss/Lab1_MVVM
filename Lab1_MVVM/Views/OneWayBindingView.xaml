<UserControl x:Class="Lab1_MVVM.Views.OneWayBindingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Lab1_MVVM.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:OneWayBindingViewModel/>
    </UserControl.DataContext>

    <StackPanel Margin="20">
        <TextBlock Text="Односторонняя привязка (OneWay)" FontWeight="Bold" FontSize="16"/>

        <GroupBox Header="OneWay - данные идут только от источника к цели" Margin="0,10">
            <StackPanel>
                <TextBlock Text="TextBox с привязкой OneWay (изменения в TextBox НЕ влияют на VM):" />
                <TextBox Text="{Binding SourceData, Mode=OneWay}" Margin="0,5,0,10" Background="LightYellow"/>

                <TextBlock Text="Значение в ViewModel (обновляется только из кода):" />
                <TextBlock Text="{Binding SourceData}" Background="LightCyan" Margin="0,5" Padding="5"/>

                <Button Content="Обновить SourceData в ViewModel" Click="UpdateSource_Click" Margin="0,10,0,0"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Пояснение" Margin="0,10">
            <TextBlock TextWrapping="Wrap">
                <Run FontWeight="Bold">Mode=OneWay</Run> – изменения в UI не отправляются обратно в ViewModel.
                <LineBreak/>
                Попробуйте изменить текст в жёлтом TextBox – ниже текст не изменится, так как привязка односторонняя.
                <LineBreak/>
                Только нажатие кнопки (обновление свойства в коде) изменит отображение.
            </TextBlock>
        </GroupBox>
    </StackPanel>
</UserControl>
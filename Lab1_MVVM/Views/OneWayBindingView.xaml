<UserControl x:Class="Lab1_MVVM.Views.OneWayBindingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Lab1_MVVM.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:OneWayBindingViewModel/>
    </UserControl.DataContext>
    <StackPanel Margin="20">
        <TextBlock Text="Односторонняя привязка (OneWay)" FontWeight="Bold" FontSize="16"/>

        <GroupBox Header="{DynamicResource OneWay_Description}" Margin="0,10">
            <StackPanel>
                <TextBlock Text="{DynamicResource OneWay_TextBoxLabel}" />
                <TextBox Text="{Binding SourceData, Mode=OneWay}" Margin="0,5,0,10" Background="LightYellow"/>
                <TextBlock Text="{DynamicResource OneWay_VMValueLabel}" />
                <TextBlock Text="{Binding SourceData}" Background="LightCyan" Margin="0,5" Padding="5"/>
                <Button Content="{DynamicResource OneWay_Button}" Click="UpdateSource_Click" Margin="0,10,0,0"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="{DynamicResource Explanation_Header}" Margin="0,10">
            <TextBlock TextWrapping="Wrap" Text="{DynamicResource OneWay_Explanation}"/>
        </GroupBox>
    </StackPanel>
</UserControl>